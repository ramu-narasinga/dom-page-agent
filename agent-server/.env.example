ANTHROPIC_API_KEY=sk-ant-your-key-here
# Defaults to http://localhost:3000 if unset. Comma-separated for multiple origins.
# Override only for a deliberate one-off demo against a real external site, then set
# it back — this is a security boundary, not something to leave wide open.
# ALLOWED_ORIGIN=http://localhost:3000,https://thinkthroo.com
